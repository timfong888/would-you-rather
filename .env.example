# RevenueCat sandbox/dev API key — get from RevenueCat dashboard → API Keys → sandbox
# Add the real value to Blocks secrets as REVENUE_CAT_DEV_KEY, then set it here as:
EXPO_PUBLIC_REVENUE_CAT_DEV_KEY=appl_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
